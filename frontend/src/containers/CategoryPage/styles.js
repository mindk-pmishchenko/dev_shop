import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  categoryPage: {
    height: '100%'
  },
  field: {
    fontSize: '26px'
  }
}))

export default useStyles
